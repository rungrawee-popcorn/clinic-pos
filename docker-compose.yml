services:
  postgres:
    image: postgres:16
    container_name: clinic-postgres
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: clinic
    ports:
      - "5432:5432"